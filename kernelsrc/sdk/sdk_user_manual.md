# SVCrtOS SDK ??????

## 1. ????

SVCrtOS ???????????SDK?????????**????????**??**??????????**??

| SDK | ?????? | ??????? | ???? | ?????? |
|-----|---------|---------|------|---------|
| **App SDK** | ??¨®?????? | `svcrt.h` | ??MCU???? | ??????SVC????? |
| **Driver SDK** | ?υτ?????????? | `svcrt_driver_sdk.h` | ??MCU???? | ????/???? |

????SDK??**???????¦Κ?MCU????????????????**?????????????????????????????????

---

## ????????App SDK

### 2. App SDK ???

App SDK ??????SVCrtOS??¨®??????????????????????¨®??????????????????SVC?????????????????????????????????????????????

**???????**
- ??¨®?????"????"??????????????????ROM????????????
- ??¨®???????????????????????????
- ???????????????υτ????????????

### 3. App SDK ????Λ

```
sdk/app_sdk/
?????? svcrt.h              ?? ???API?????????¦·???????????????
?????? svcrt_types.h        ?? ??????????‰Ν??svcrt.h?????????
?????? svcrt_app_config.h   ?? ?????????????
?????? svcrt_oslib.c        ?? SVC?????΅Β?????
?????? svcrt_app_main.c     ?? ????????‰Ν??AppMain?????‰Ξ
?????? svcrt_app_start.s    ?? ?????????????
?????? examples/
    ?????? example_app.c    ?? ??????????
```

### 4. ????????5????????????

#### ????1??????????

???C?????????SVCrtOS App SDK??????

```c
#include "svcrt.h"
```

#### ????2????? AppMain()

`AppMain()` ????¨®???????????????????C????? `main()`??

```c
void AppMain(void)
{
    // ?????????
}
```

#### ????3?????OS API

?? `AppMain()` ?§Φ???OS????

```c
void AppMain(void)
{
    int32 led = svcrt_dev_open("LED", 0);

    while(1)
    {
        uint8 val = 1;
        svcrt_dev_write(led, &val, 1);
        svcrt_task_wait(500);

        val = 0;
        svcrt_dev_write(led, &val, 1);
        svcrt_task_wait(500);
    }
}
```

#### ????4?????΅Β???????

?? `svcrt_app_config.h` ?§Ψ?????????????

```c
svcrt_app_cfg_t svcrt_app_cfg_table[] = {
    {
        .ram_start       = 0x20000000,   // RAM??????
        .ram_size        = 0x1000,       // RAM??§³(4KB)
        .stack_size      = 0x400,        // ???§³(1KB)
        .rom_start       = 0x08020000,   // ROM??????
        .rom_size        = 0x20000,      // ROM??§³(128KB)
        .period          = 1000,         // ????????(1??)
        .priority        = 10,           // ?????
        .shm_attri       = 0             // ??????????????
    }
};
int32 svcrt_app_count = 1;
```

#### ????5???????????

??????????????????????????????????????ROM???????????¨ΐ?

### 5. App SDK API ???

#### 5.1 ?υτ????API

##### `svcrt_dev_open` - ???υτ

```c
int32 svcrt_dev_open(char* name, uint32 param);
```

| ???? | ??? |
|------|------|
| `name` | ?υτ????????????? `"COM1"`, `"LED"`, `"SPI1"` |
| `param` | ??????????ν???????????‰Ν?»“????? |

| ????? | ??? |
|--------|------|
| ?? 0 | ?υτ???????????????? |
| -1 | ?υτ¦Δ????????? |

**?????**
```c
int32 com1 = svcrt_dev_open("COM1", 115200);  // ??115200??????????1
int32 led  = svcrt_dev_open("LED", 0);         // ??LED?υτ
int32 spi  = svcrt_dev_open("SPI1", 0);        // ??SPI1

if(com1 < 0) {
    // ?υτ????????
}
```

##### `svcrt_dev_close` - ????υτ

```c
int32 svcrt_dev_close(int32 handle);
```

| ???? | ??? |
|------|------|
| `handle` | `svcrt_dev_open` ??????υτ??? |

| ????? | ??? |
|--------|------|
| 0 | ????? |
| -1 | ?????§Ή??????? |

**?????**
```c
int32 com1 = svcrt_dev_open("COM1", 115200);
// ... ????υτ ...
svcrt_dev_close(com1);  // ????υτ???????
```

##### `svcrt_dev_read` - ???????

```c
int32 svcrt_dev_read(int32 handle, void *pdata, int32 len);
```

| ???? | ??? |
|------|------|
| `handle` | `svcrt_dev_open` ??????υτ??? |
| `pdata` | ??????????????? |
| `len` | ?????????????? |

| ????? | ??? |
|--------|------|
| > 0 | ???????????? |
| 0 | ???????? |
| -1 | ?????? |

**?????**
```c
uint8 buf[64];
int32 len = svcrt_dev_read(com1, buf, 64);
if(len > 0) {
    // ???????????????
}
```

##### `svcrt_dev_write` - §Υ??????

```c
int32 svcrt_dev_write(int32 handle, void *pdata, int32 len);
```

| ???? | ??? |
|------|------|
| `handle` | ?υτ??? |
| `pdata` | ?????????????????? |
| `len` | ??????????? |

| ????? | ??? |
|--------|------|
| ?? 0 | ???§Υ???????? |
| -1 | §Υ????? |

**?????**
```c
uint8 msg[] = "Hello";
svcrt_dev_write(com1, msg, 5);
```

##### `svcrt_dev_ctrl` - ?υτ????

```c
int32 svcrt_dev_ctrl(int32 handle, uint32 code, uint32 value);
```

| ???? | ??? |
|------|------|
| `handle` | ?υτ??? |
| `code` | ???????????????????? |
| `value` | ????????????????????? |

**?????????**

| ?????? | ? | ??? |
|--------|-----|------|
| `SVCRT_DEV_CTRL_OPEN` | 0x0001 | ???υτ |
| `SVCRT_DEV_CTRL_CLOSE` | 0x0002 | ????υτ |
| `SVCRT_DEV_CTRL_SET_BAUD` | 0x0010 | ???¨°????? |
| `SVCRT_DEV_CTRL_SET_MODE` | 0x0011 | ???¨΄????? |
| `SVCRT_DEV_CTRL_GET_STATUS` | 0x0020 | ????υτ?? |
| `SVCRT_DEV_CTRL_RESET` | 0x0030 | ??¦Λ?υτ |

**?????**
```c
// ???¨°??????9600
svcrt_dev_ctrl(com1, SVCRT_DEV_CTRL_SET_BAUD, 9600);

// ????υτ??
int32 status = svcrt_dev_ctrl(com1, SVCRT_DEV_CTRL_GET_STATUS, 0);

// ??¦Λ?υτ
svcrt_dev_ctrl(com1, SVCRT_DEV_CTRL_RESET, 0);
```

#### 5.2 ???????API

##### `svcrt_task_wait` - ??????????

```c
void svcrt_task_wait(uint32 ms);
```

?????????????????????????????????WAIT??????????????????READY??

**?????**
```c
while(1) {
    do_work();
    svcrt_task_wait(100);  // ?100ms??????
}
```

##### `svcrt_task_wait_period` - ??????????

```c
void svcrt_task_wait_period(void);
```

?????????????????????????????????????????????????

**?????**
```c
while(1) {
    periodic_work();
    svcrt_task_wait_period();  // ????????????
}
```

##### `svcrt_task_delay` - ??????

```c
void svcrt_task_delay(uint32 us);
```

???????????**?????¨®?CPU**???????RUNNING?????????????‚A??????

**?????**
```c
// ???????????????100???
svcrt_task_delay(100);
```

> **???** `svcrt_task_delay` ???????????CPU??????????????? `svcrt_task_wait`??

##### `svcrt_task_kill` - ??????????

```c
void svcrt_task_kill(void);
```

????????§Φ??????????????INVALID????????????

**?????**
```c
if(fatal_error) {
    svcrt_task_kill();  // ???????????????????
}
```

#### 5.3 ?????API

##### `svcrt_get_time_ms` - ????????

```c
uint32 svcrt_get_time_ms(void);
```

?????????????????????????

**?????**
```c
uint32 start = svcrt_get_time_ms();
do_work();
uint32 elapsed = svcrt_get_time_ms() - start;
```

##### `svcrt_get_cpu_usage` - ???CPU????

```c
uint32 svcrt_get_cpu_usage(void);
```

????CPU???????0~1024??¦¶??????1024???100%???????

**?????**
```c
uint32 load = svcrt_get_cpu_usage();
// load / 1024 * 100 = CPU????????
```

#### 5.4 ???API

##### `svcrt_event_create` - ???????

```c
int32 svcrt_event_create(char* name);
```

| ???? | ??? |
|------|------|
| `name` | ????????????????15????? |

| ????? | ??? |
|--------|------|
| ?? 0 | ?????? |
| -1 | ??????????????????? |

##### `svcrt_event_wait` - ??????

```c
void svcrt_event_wait(int32 handle, int32 timeout);
```

| ???? | ??? |
|------|------|
| `handle` | `svcrt_event_create` ??????????? |
| `timeout` | ??????(????)??0??????????????? |

##### `svcrt_event_set` - ???????

```c
void svcrt_event_set(int32 handle);
```

???????§Φ?????????????

**????????????**

```c
// ??????????
void AppMain(void)
{
    int32 evt = svcrt_event_create("data_ready");

    while(1)
    {
        produce_data();
        svcrt_event_set(evt);         // ???????????????
        svcrt_task_wait(100);
    }
}

// ?????????????????????
void AppMain(void)
{
    int32 evt = svcrt_event_create("data_ready");

    while(1)
    {
        svcrt_event_wait(evt, 0);    // ??????????
        consume_data();
    }
}
```

#### 5.5 ??????API???????????????

##### ?????

????????????????????????????????

```c
int32 svcrt_sem_create(char *name, int32 init_count);  // ??????init_count????????
int32 svcrt_sem_wait(int32 handle, int32 timeout);     // ?????????>0???1?????????
int32 svcrt_sem_post(int32 handle);                    // ????????????????+1??
int32 svcrt_sem_delete(int32 handle);                  // ???
```

| ????? | ??? |
|--------|------|
| ?? 0 | ?????create?????????????? |
| -1 | ??? |

**??????-???????????**
```c
int32 sem = svcrt_sem_create("buf_sem", 0);

// ??????
produce();
svcrt_sem_post(sem);          // ????????

// ??????
svcrt_sem_wait(sem, 0);       // ???????
consume();
```

##### ??????

????????????????????????????????????**????????**?????????????????

```c
int32 svcrt_mutex_create(char *name);                  // ????
int32 svcrt_mutex_lock(int32 handle, int32 timeout);   // ???????????????
int32 svcrt_mutex_unlock(int32 handle);                // ????????????????????
int32 svcrt_mutex_delete(int32 handle);                // ???
```

**??????????????**
```c
int32 mtx = svcrt_mutex_create("dev_mtx");

svcrt_mutex_lock(mtx, 0);     // ?????????
shared_dev_access();          // ?????????????????
svcrt_mutex_unlock(mtx);      // ????????
```

> **????????**????????????????????????????????§Φ?????????
> ????????????????????????????????????????????????????????
>
> **???**????????????????????????????? lock ???? -1??

### 6. App SDK ???????

#### 6.1 ??????????

```c
#include "svcrt.h"

void AppMain(void)
{
    int32 com1 = svcrt_dev_open("COM1", 115200);
    uint8 buf[128];

    while(1)
    {
        int32 len = svcrt_dev_read(com1, buf, 128);
        if(len > 0)
        {
            svcrt_dev_write(com1, buf, len);
        }
        svcrt_task_wait(10);
    }
}
```

#### 6.2 LED??? + ??????

```c
#include "svcrt.h"

void AppMain(void)
{
    int32 led = svcrt_dev_open("LED", 0);
    int32 evt = svcrt_event_create("tick");
    uint8 state = 0;

    while(1)
    {
        state = !state;
        svcrt_dev_write(led, &state, 1);
        svcrt_event_set(evt);
        svcrt_task_wait(500);
    }
}
```

#### 6.3 ???υτ§Ώ??

```c
#include "svcrt.h"

void AppMain(void)
{
    int32 com1 = svcrt_dev_open("COM1", 115200);
    int32 led  = svcrt_dev_open("LED", 0);
    int32 spi  = svcrt_dev_open("SPI1", 0);
    int32 evt  = svcrt_event_create("cmd_recv");
    uint8 cmd_buf[32];
    uint8 spi_buf[64];

    while(1)
    {
        int32 len = svcrt_dev_read(com1, cmd_buf, 32);
        if(len > 0)
        {
            // ????????
            if(cmd_buf[0] == 0x01) {
                // ???SPI????
                svcrt_dev_read(spi, spi_buf, 64);
                svcrt_dev_write(com1, spi_buf, 64);
            }
            else if(cmd_buf[0] == 0x02) {
                // ?§έ?LED
                uint8 val = cmd_buf[1];
                svcrt_dev_write(led, &val, 1);
            }

            svcrt_event_set(evt);
        }
        svcrt_task_wait(20);
    }
}
```

### 7. App SDK ???Z

#### 7.1 ????????????

1. **?????????????????** ?? ????????????????? `svcrt_dev_open/read/write/ctrl`
2. **?????????????????** ?? ??? `svcrt_task_wait` ?? `svcrt_event_wait` ?¨®?CPU
3. **AppMain() ???????** ?? ??????????????
4. **????????** ?? ????????????????????????????????
5. **?????????????????** ?? ???? `svcrt.h` ????????API

#### 7.2 ?????????

```c
void AppMain(void)
{
    // ???1???????
    int32 dev1 = svcrt_dev_open("DEV1", 0);
    int32 evt  = svcrt_event_create("my_evt");

    // ???2???????
    while(1)
    {
        // ???????
        int32 len = svcrt_dev_read(dev1, buf, sizeof(buf));

        // ????????
        if(len > 0) {
            process_data(buf, len);
        }

        // ??????
        svcrt_dev_write(dev1, result, result_len);

        // ??????????
        svcrt_task_wait(period_ms);
    }
}
```

---

## ????????Driver SDK

### 8. Driver SDK ???

Driver SDK ??????SVCrtOS?υτ???????????????????????? `svcrt_drv_register()` ??????????υτ??????¨®?????? `svcrt_dev_open()` ??API??¨Ά?

Driver SDK ???**?????????**??????????????????????

| ?? | ???? | ????? | ??????? | ??????? | ???¨®??? |
|------|--------|--------|----------|----------|----------|
| **????** | ??????????? | ???? | ????? `.lib` | ??????????? | ??????????????????????? |
| **????** | `SVCRT_DRV_USER_MODE` | ???? | ??????? `.bin` | ????????? | ???????????????§Ώ?????? |

**?????????**
- **??????** ?? ??????????????????/??????????????????
- **?????** ?? ???????????????? `svcrt_dev_drv_t` ???
- **????????** ?? ????SDK???????¦Κ?MCU???????????????????????????
- **??????** ?? ????????? `extern` ??????????????????????? SVC 0x14 ?????????????

### 9. Driver SDK ????Λ

```
sdk/driver_sdk/
?????? svcrt_driver_sdk.h       ?? ????????API??????¦·???????????????
?????? svcrt_types.h            ?? ??????????‰Ν??svcrt_driver_sdk.h?????????
?????? svcrt_driver_bridge.c    ?? ?????????????????????????
?????? svcrt_drv_main.c         ?? ??????????????DrvMain?????‰Ξ
?????? svcrt_drv_start.s        ?? ??????????????????
?????? svcrt_drv_oslib.c        ?? ????????OS???????SVC?????
?????? examples/
    ?????? example_led_drv.c    ?? LED???????
    ?????? example_uart_drv.c   ?? UART??????????????+???¦Λ??‰Ξ
    ?????? example_spi_drv.c    ?? SPI???????????
    ?????? example_i2c_drv.c    ?? I2C???????????
    ?????? example_gpio_drv.c   ?? ???GPIO???????
    ?????? example_adc_drv.c    ?? ADC???????????
```

**??????????????????**

| ??? | ?????? | ?????? | ??? |
|------|-----------|-----------|------|
| `svcrt_driver_sdk.h` | ? ???? | ? ???? | ????????¦·?????????????? |
| `svcrt_types.h` | ? ???? | ? ???? | ??????????ΘΙSDK????? |
| `svcrt_driver_bridge.c` | ? ???? | ? ???? | ????????????????§έ????΅Β?? |
| `svcrt_drv_main.c` | ?? ????? | ? ???? | ?? `DrvMain()` ???? `main()` |
| `svcrt_drv_start.s` | ?? ????? | ? ???? | ?????????????? |
| `svcrt_drv_oslib.c` | ?? ????? | ? ???? | ?????????????OS??? |

### 10. ???????????????????

???????????4????????????????????????????????5???????????????¨Ά?

#### ????1?????????????

???C?????????Driver SDK??????

```c
#include "svcrt_driver_sdk.h"
```

#### ????2???????υτ?????

```c
typedef struct {
    svcrt_dev_hdr_t hdr;     // ?????????????
    uint32  my_reg;          // ?υτ????????
    uint8   state;           // ?υτ??
} my_dev_obj_t;
```

> **?????** ?υτ????????????????????? `svcrt_dev_hdr_t`??

#### ????3?????5????????????

```c
static svcrt_dev_hdr_t* my_drv_open(uint32 devid, uint32 param)
{
    // ?????????????¨°???
    // ?????υτ???????
}

static int32 my_drv_close(svcrt_dev_hdr_t *obj)
{
    // ????υτ????????
}

static int32 my_drv_read(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len)
{
    // ???υτ???????
    // ????????????????
}

static int32 my_drv_write(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len)
{
    // ???υτ§Υ??????
    // ???????§Υ????????
}

static int32 my_drv_ctrl(svcrt_dev_hdr_t *obj, uint32 code, uint32 value)
{
    // ?υτ???????
    // ????code??§Σ??????
}
```

#### ????4????????????????

```c
static svcrt_dev_drv_t my_drv = {
    my_drv_open,
    my_drv_close,
    my_drv_read,
    my_drv_write,
    my_drv_ctrl
};
```

#### ????5??????????????????

##### ??????

?????????????‰???????????

```c
int32 my_drv_install(void)
{
    return svcrt_drv_register("MYDEV", &my_drv, 0);
}
```

**?????????**
- ???‰Ψ?????????????????
- ?????????`my_drv.c` + `svcrt_driver_bridge.c`
- ????΅¤????`sdk/driver_sdk/`
- ???????????? `.lib`?????????????
- ?????????? `board/` ??? `svcrt_dev_board_init()` ?§Φ??? `my_drv_install()`

##### ??????

??? `DrvMain()` ??????

```c
void DrvMain(void)
{
    svcrt_drv_register("MYDEV", &my_drv, 0);

    while(1)
    {
        svcrt_task_wait(1000);
    }
}
```

**?????????**
- ???‰Ψ`SVCRT_DRV_USER_MODE`
- ?????????`my_drv.c` + `svcrt_driver_bridge.c` + `svcrt_drv_oslib.c` + `svcrt_drv_main.c` + `svcrt_drv_start.s`
- ????΅¤????`sdk/driver_sdk/`
- ?????????????? `.bin`??????????ROM????
- ???????????????????????? `DrvMain()`????? SVC 0x14 ???????

> **???** ?????????? `DrvMain()` ??????????????????????????????? `svcrt_task_wait()` ?¨®?CPU??

### 11. Driver SDK API ???

#### 11.1 ??????? svcrt_dev_drv_t

```c
typedef struct {
    svcrt_drv_open_func    drv_open;    // ???υτ
    svcrt_drv_close_func   drv_close;   // ????υτ
    svcrt_drv_read_func    drv_read;    // ???????
    svcrt_drv_write_func   drv_write;   // §Υ??????
    svcrt_drv_ctrl_func    drv_ctrl;    // ?υτ????
} svcrt_dev_drv_t;
```

???????????????‰Ψ

```c
typedef svcrt_dev_hdr_t* (*svcrt_drv_open_func)(uint32 devid, uint32 param);
typedef int32            (*svcrt_drv_close_func)(svcrt_dev_hdr_t *obj);
typedef int32            (*svcrt_drv_read_func)(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len);
typedef int32            (*svcrt_drv_write_func)(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len);
typedef int32            (*svcrt_drv_ctrl_func)(svcrt_dev_hdr_t *obj, uint32 code, uint32 value);
```

#### 11.2 drv_open - ???υτ

```c
svcrt_dev_hdr_t* drv_open(uint32 devid, uint32 param);
```

| ???? | ??? |
|------|------|
| `devid` | ????????υτ??????????????? |
| `param` | ??????????? |

| ????? | ??? |
|--------|------|
| ??0 | ?υτ???????svcrt_dev_hdr_t*?? |
| 0 | ????? |

**??????**
- ????????????
- ?????υτ???????»“??????????????
- ?????υτ????????????????????????

#### 11.3 drv_close - ????υτ

```c
int32 drv_close(svcrt_dev_hdr_t *obj);
```

| ???? | ??? |
|------|------|
| `obj` | drv_open??????υτ???? |

| ????? | ??? |
|--------|------|
| `SVCRT_DRV_OK` (0) | ??? |
| `SVCRT_DRV_ERROR` (-1) | ??? |

#### 11.4 drv_read - ???????

```c
int32 drv_read(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len);
```

| ???? | ??? |
|------|------|
| `obj` | ?υτ???? |
| `pdata` | ???????????? |
| `len` | ?????????????? |

| ????? | ??? |
|--------|------|
| > 0 | ?????????? |
| 0 | ?????? |
| ???? | ?????? |

#### 11.5 drv_write - §Υ??????

```c
int32 drv_write(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len);
```

??????????????? drv_read??

#### 11.6 drv_ctrl - ?υτ????

```c
int32 drv_ctrl(svcrt_dev_hdr_t *obj, uint32 code, uint32 value);
```

| ???? | ??? |
|------|------|
| `obj` | ?υτ???? |
| `code` | ?????? |
| `value` | ????? |

**?????????**

| ?????? | ? | ??? |
|--------|-----|------|
| `SVCRT_DEV_CTRL_OPEN` | 0x0001 | ????? |
| `SVCRT_DEV_CTRL_CLOSE` | 0x0002 | ??? |
| `SVCRT_DEV_CTRL_SET_BAUD` | 0x0010 | ???¨°????? |
| `SVCRT_DEV_CTRL_SET_MODE` | 0x0011 | ?????? |
| `SVCRT_DEV_CTRL_GET_STATUS` | 0x0020 | ????? |
| `SVCRT_DEV_CTRL_RESET` | 0x0030 | ??¦Λ |

??????????????????????????0x0100???????

#### 11.7 ???????/???API

##### `svcrt_drv_register` - ???????

```c
int32 svcrt_drv_register(const char *name, svcrt_dev_drv_t *drv, uint32 dev_num);
```

| ???? | ??? |
|------|------|
| `name` | ?υτ???????7?????????????????????υτ |
| `drv` | ?????????? |
| `dev_num` | ????????υτ????????drv_open??devid???? |

| ????? | ??? |
|--------|------|
| 0 | ????? |
| -1 | ?υτ?????? |
| -2 | ??????§Ή????????? |

##### `svcrt_drv_unregister` - ???????

```c
int32 svcrt_drv_unregister(const char *name);
```

| ????? | ??? |
|--------|------|
| 0 | ?????? |
| -1 | ?υτ¦Δ??? |

##### `svcrt_drv_get_count` - ?????????υτ??

```c
int32 svcrt_drv_get_count(void);
```

#### 11.8 ?????????????

| ????? | ??? | ??? |
|--------|------|------|
| `SVCRT_DRV_OK` | 0 | ??????? |
| `SVCRT_DRV_ERROR` | -1 | ?????? |
| `SVCRT_DRV_BUSY` | -2 | ?υτ? |
| `SVCRT_DRV_TIMEOUT` | -3 | ??????? |
| `SVCRT_DRV_INVALID_PARAM` | -4 | ??????§Ή |

### 12. Driver SDK ?????????

Driver SDK ???????????? `SVCRT_DRV_USER_MODE` ?§έ????????????????????????????????§Υ??????????**??????**?????????????¨Ή????§έ?????

#### 12.1 ????????????

**?????????**

???? `svcrt_driver_bridge.c` ??? `extern` ??????????????????

```
svcrt_drv_register()  ??  svcrt_dev_register()    ?????????????????????
svcrt_drv_unregister() ??  svcrt_dev_unregister()  ?????????????????????
svcrt_drv_get_count()  ??  svcrt_dev_get_count()   ?????????????????????
```

**????????**

```
svcrt_driver_sdk.h  ??  svcrt_types.h  ??SDK?????????MCU??????
svcrt_driver_bridge.c  ??  svcrt_driver_sdk.h  +  extern ??????????
```

**?????????MDK???????**

| ?????? | ? |
|--------|-----|
| ???? | ?????????? |
| C??????? | `???????.c`??`svcrt_driver_bridge.c` |
| ????΅¤?? | `sdk/driver_sdk/` |
| ??????? | ??????Create Library??????? `.lib` |

**????????**

1. ?????? `.lib` ???????? MDK ???????§Ò???
2. ?? `board/` ??? `svcrt_dev_board_init()` ?§Φ?????????????????

```c
extern int32 my_drv_install(void);

void svcrt_dev_board_init(void)
{
    svcrt_dev_register("COM1", &usart_drv, 0);
    svcrt_dev_register("LED",  &led_drv,  0);
    my_drv_install();                    // ????????????
}
```

**?????**
- ??????????????????????????
- ???????????????????????

**?????**
- ???????????????????????????
- ????bug?????????????????

#### 12.2 ????????SVCRT_DRV_USER_MODE??

**?????????**

???? `svcrt_driver_bridge.c` ??? SVC ????????????????

```
svcrt_drv_register()  ??  __svc(0x14)  ??  SVC_Server  ??  svcrt_dev_register()
svcrt_drv_unregister() ??  __svc(0x14)  ??  SVC_Server  ??  svcrt_dev_unregister()
svcrt_drv_get_count()  ??  __svc(0x14)  ??  SVC_Server  ??  svcrt_dev_get_count()
```

SVC 0x14 ??????????

| p[0] | ???? | ???? |
|------|------|------|
| 1 | ??????? | p[1]=name, p[2]=drv???, p[3]=dev_num |
| 2 | ??????? | p[1]=name |
| 3 | ????υτ?? | ?? |

**????????**

```
svcrt_driver_sdk.h  ??  svcrt_types.h  ??SDK?????????MCU??????
svcrt_driver_bridge.c  ??  svcrt_driver_sdk.h  ??SVC????????????????
svcrt_drv_oslib.c      ??  svcrt_driver_sdk.h  ??SVC????????????????
svcrt_drv_main.c       ??  svcrt_driver_sdk.h  ?????????????
svcrt_drv_start.s      ??  ??????
```

**?????????MDK???????**

| ?????? | ? |
|--------|-----|
| ???? | `SVCRT_DRV_USER_MODE` |
| C??????? | `???????.c`??`svcrt_driver_bridge.c`??`svcrt_drv_oslib.c`??`svcrt_drv_main.c` |
| ?????? | `svcrt_drv_start.s` |
| ????΅¤?? | `sdk/driver_sdk/` |
| ??????? | ??????????????Create Executable??????? `.bin/.hex` |
| ?????? | ??????ROM/RAM???????scatter??? |

**?????????????OS API??**

??? `svcrt_drv_oslib.c` ?????? App SDK ????????

| API | ??? |
|-----|------|
| `svcrt_task_wait(ms)` | ??????????? |
| `svcrt_task_wait_period()` | ?????????????? |
| `svcrt_task_delay(us)` | ?????? |
| `svcrt_get_time_ms()` | ???????? |
| `svcrt_event_create(name)` | ??????? |
| `svcrt_event_wait(handle, timeout)` | ?????? |
| `svcrt_event_set(handle)` | ??????? |

**?????**
- ???????????????????????
- ????bug??????????????MPU????
- ????????/?I?????????΅ΐ??????

**?????**
- ??????????????????
- SVC??????????????
- ???????????????????

#### 12.3 ????????

| ???? | ????? | ??? |
|------|---------|------|
| UART/SPI/I2C ????????????????????????? | ???? | ???????§Υ????? |
| LED/GPIO ??????????? | ???? | ?????????????§Ή??? |
| ?????/????§Ώ???????????? | ???? | ???????????????????????? |
| ?????????????/????? | ???? | ???????????MPU???????? |
| ????????????? | ???? | ???Ψh???????????? |

### 13. Driver SDK ???????

#### 13.1 SPI???????

```c
#include "svcrt_driver_sdk.h"

#define SPI1_BASE    0x40013000

typedef struct {
    svcrt_dev_hdr_t hdr;
    uint32  base;
    uint32  speed;
    uint8   mode;
} spi_dev_obj_t;

static spi_dev_obj_t spi_dev = {{0}, SPI1_BASE, 1000000, 0};

static svcrt_dev_hdr_t* spi_drv_open(uint32 devid, uint32 param)
{
    spi_dev.speed = param ? param : 1000000;
    spi_dev.mode = 0;
    spi_dev.hdr.block_size = 1;

    // ????SPI???
    // SPI_Init(spi_dev.base, spi_dev.speed, spi_dev.mode);

    return (svcrt_dev_hdr_t*)&spi_dev;
}

static int32 spi_drv_close(svcrt_dev_hdr_t *obj)
{
    // SPI_DeInit(((spi_dev_obj_t*)obj)->base);
    return SVCRT_DRV_OK;
}

static int32 spi_drv_read(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len)
{
    spi_dev_obj_t *p = (spi_dev_obj_t*)obj;
    int32 i;
    for(i = 0; i < len; i++)
    {
        // pdata[i] = SPI_Transfer(p->base, 0xFF);
    }
    return len;
}

static int32 spi_drv_write(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len)
{
    spi_dev_obj_t *p = (spi_dev_obj_t*)obj;
    int32 i;
    for(i = 0; i < len; i++)
    {
        // SPI_Transfer(p->base, pdata[i]);
    }
    return len;
}

static int32 spi_drv_ctrl(svcrt_dev_hdr_t *obj, uint32 code, uint32 value)
{
    spi_dev_obj_t *p = (spi_dev_obj_t*)obj;
    switch(code)
    {
    case SVCRT_DEV_CTRL_SET_MODE:
        p->mode = value;
        return SVCRT_DRV_OK;
    case SVCRT_DEV_CTRL_SET_BAUD:
        p->speed = value;
        return SVCRT_DRV_OK;
    case SVCRT_DEV_CTRL_GET_STATUS:
        return p->speed;
    default:
        return SVCRT_DRV_ERROR;
    }
}

static svcrt_dev_drv_t spi_drv = {
    spi_drv_open,
    spi_drv_close,
    spi_drv_read,
    spi_drv_write,
    spi_drv_ctrl
};

int32 spi_drv_install(void)
{
    return svcrt_drv_register("SPI1", &spi_drv, 0);
}
```

#### 13.2 ?????????????????????????υτ??

```c
#include "svcrt_driver_sdk.h"

typedef struct {
    svcrt_dev_hdr_t hdr;
    uint32  uart_base;
    uint32  baudrate;
} uart_dev_obj_t;

#define UART_MAX  3
static uart_dev_obj_t uart_devs[UART_MAX] = {
    {{0}, 0x40011000, 115200},  // UART1
    {{0}, 0x40004400, 115200},  // UART2
    {{0}, 0x40004800, 115200},  // UART3
};

static svcrt_dev_hdr_t* uart_drv_open(uint32 devid, uint32 param)
{
    uart_dev_obj_t *p;
    if(devid >= UART_MAX) return 0;

    p = &uart_devs[devid];
    p->baudrate = param ? param : 115200;
    p->hdr.block_size = 1;

    // UART_Init(p->uart_base, p->baudrate);
    return (svcrt_dev_hdr_t*)p;
}

static int32 uart_drv_close(svcrt_dev_hdr_t *obj) { return SVCRT_DRV_OK; }
static int32 uart_drv_read(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len) { return 0; }
static int32 uart_drv_write(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len) { return len; }

static int32 uart_drv_ctrl(svcrt_dev_hdr_t *obj, uint32 code, uint32 value)
{
    uart_dev_obj_t *p = (uart_dev_obj_t*)obj;
    if(code == SVCRT_DEV_CTRL_SET_BAUD) {
        p->baudrate = value;
        return SVCRT_DRV_OK;
    }
    return SVCRT_DRV_ERROR;
}

static svcrt_dev_drv_t uart_drv = {
    uart_drv_open, uart_drv_close,
    uart_drv_read, uart_drv_write, uart_drv_ctrl
};

void uart_drv_install_all(void)
{
    svcrt_drv_register("COM1", &uart_drv, 0);  // devid=0 ?? uart_devs[0]
    svcrt_drv_register("COM2", &uart_drv, 1);  // devid=1 ?? uart_devs[1]
    svcrt_drv_register("COM3", &uart_drv, 2);  // devid=2 ?? uart_devs[2]
}
```

### 13. Driver SDK ???Z

#### 13.1 ????????????

1. **?υτ????????????????? `svcrt_dev_hdr_t`** ?? ????????????????υτ
2. **drv_open ????????§Ή???υτ???????** ?? ????0????????
3. **drv_read/drv_write ???????????????** ?? ??????????????
4. **?§Ψ?????§Σ??????????API** ?? ?§Ψ?????????????????
5. **??? `svcrt_fifo_t` ?????§Ψ????????????** ?? ???????§Ψ??????????υτ????

#### 13.2 ?????????

```c
// ??????????????????????
#define DRV_GET_OBJ(obj, type)  ((type*)(obj))

// ????????????????
static my_dev_obj_t* get_dev_obj(svcrt_dev_hdr_t *hdr)
{
    return (my_dev_obj_t*)hdr;
}
```

#### 13.3 ?§Ψ?????????§Ώ??

?????????????§Ψ??§ί?????????????????????§Υ??????????? FIFO ????????

```c
static svcrt_fifo_t rx_fifo;
static uint8 rx_buf[256];

static svcrt_dev_hdr_t* my_drv_open(uint32 devid, uint32 param)
{
    svcrt_fifo_init(&rx_fifo, rx_buf, 256);
    // ...
}

// ?§Ψ??????
void USART1_IRQHandler(void)
{
    if(USART_GetITStatus(USART1, USART_IT_RXNE))
    {
        uint8 data = USART_ReceiveData(USART1);
        svcrt_fifo_write(&rx_fifo, &data, 1);
    }
}

// ???????????????????????????
static int32 my_drv_read(svcrt_dev_hdr_t *obj, uint8 *pdata, int32 len)
{
    return svcrt_fifo_read(&rx_fifo, pdata, len);
}
```

---

## ?????API ????

### App SDK API ???

| ???? | API | ??? |
|------|-----|------|
| ?υτ | `svcrt_dev_open(name, param)` | ???υτ |
| ?υτ | `svcrt_dev_close(handle)` | ????υτ |
| ?υτ | `svcrt_dev_read(handle, buf, len)` | ??????? |
| ?υτ | `svcrt_dev_write(handle, buf, len)` | §Υ?????? |
| ?υτ | `svcrt_dev_ctrl(handle, code, value)` | ?υτ???? |
| ???? | `svcrt_task_wait(ms)` | ?????? |
| ???? | `svcrt_task_wait_period()` | ?????????? |
| ???? | `svcrt_task_delay(us)` | ?????? |
| ???? | `svcrt_task_kill()` | ??????? |
| ??? | `svcrt_event_create(name)` | ??????? |
| ??? | `svcrt_event_wait(handle, timeout)` | ?????? |
| ??? | `svcrt_event_set(handle)` | ??????? |
| ?? | `svcrt_get_time_ms()` | ????? |
| ?? | `svcrt_get_cpu_usage()` | CPU???? |

### Driver SDK API ???

| API | ??? |
|-----|------|
| `svcrt_drv_register(name, drv, dev_num)` | ??????? |
| `svcrt_drv_unregister(name)` | ??????? |
| `svcrt_drv_get_count()` | ????υτ?? |
| `svcrt_fifo_init(fifo, buf, size)` | ?????FIFO |
| `svcrt_fifo_read(fifo, buf, len)` | ??FIFO??? |
| `svcrt_fifo_write(fifo, buf, len)` | §Υ??FIFO |

### ?????????

| ?????? | ? | ??? |
|--------|-----|------|
| `SVCRT_DRV_OK` | 0 | ??? |
| `SVCRT_DRV_ERROR` | -1 | ?????? |
| `SVCRT_DRV_BUSY` | -2 | ?υτ? |
| `SVCRT_DRV_TIMEOUT` | -3 | ??? |
| `SVCRT_DRV_INVALID_PARAM` | -4 | ??????§Ή |
| `svcrt_dev_open` ???? -1 | -1 | ?υτ¦Δ??? |
| `svcrt_event_create` ???? -1 | -1 | ??????? |
